extends layouts/style.pug

block content 
  .row.mt-3
  .col-sm-4.mx-auto
    h3.text-xs-center Authentication form
    form(action='/user/terminal' method='post')
      .row.form-group
        .col-sm
          input.form-control(name='nickname' placeholder='nickname' value=form.nickname)
          if errors.nickname
            .text-warning= errors.nickname
      .row.form-group
        .col-sm
          input.form-control(name='password' type='password' placeholder='password')
          if errors.password
            .text-warning= errors.password
      .form-group
        button.btn.btn-success.btn-block(type='submit' style='background-color:#C0C0C0;') Sign up
        .etc-login-form
          p 
            | New user? 
            a.nav-nav(href="/user/new") Create account